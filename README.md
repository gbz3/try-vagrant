try-vagrant
===========

~/.bashrc
---------
```
alias ls='ls --show-control-chars'
alias ll='ls -l'
alias rm='rm -i'
```

UTF-8 で日本語を表示できるようフォントを変更
--------------------------------------------
```
[HKEY_CURRENT_USER/Console/Git Bash]
  "Lucida Console" -> "ＭＳ　ゴシック"
```

